import DocCardList from '@theme/DocCardList';

# IT Data Types

The IT protocol defines a few special encodings for certain data types transmitted over the wire. Generally, these
data types are designed to fit within the limited size constraints of the IT Protocol, but may also exist for 
technical reasons.

A list of known and documented data types are available below:

<DocCardList />